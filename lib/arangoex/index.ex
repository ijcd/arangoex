defmodule Arangoex.Index do
  @moduledoc "ArangoDB Index methods"

  # GET /_api/index Read all indexes of a collection
  # POST /_api/index#fulltext Create fulltext index
  # POST /_api/index#general Create index
  # POST /_api/index#geo Create geo-spatial index
  # POST /_api/index#hash Create hash index
  # POST /_api/index#persistent Create a persistent index
  # POST /_api/index#skiplist Create skip list
  # DELETE /_api/index/{index-handle} Delete index
  # GET /_api/index/{index-handle} Read index
end
  
